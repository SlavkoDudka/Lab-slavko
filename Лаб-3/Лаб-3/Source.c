#include<stdio.h>
#include<math.h>

 /*int main() { // 1.A

	int f, r, e;

	printf("Enter f, r:\n");
	scanf_s("%d, %d", &f, &r);

	if (f==5)
	{
		e=3*f+2*r;
		printf("e=%d\n", e);
	}
	
	if (f==0)
	{
		e = sqrt((8*r)/2*f-4);
		printf("e=%d\n", e);
	}

	if (f==-5)
	{
		e=f*r-9;
		printf("e=%d\n", e);
	}
       else
	{
		printf("it is not possible to evaluate the value of the expression");
	}
       return 0;
} */

/*int main() { //1.Á)

	int f, r, e;


	printf("Enter f, r:\n");
	scanf_s("%d, %d", &f, &r);

	if (f==5)
	{
		e = 3 * f + 2 * r;
		printf("e=%d\n", e);
	}

	else if (f==0)
	{
		e = sqrt((8 * r) / 2 * f - 4);
		printf("e=%d\n", e);
	}

	else if (f==-5)
	{
		e = f * r - 9;
		printf("e=%d\n", e);
	}


	else
	{
		printf("it is not possible to evaluate the value of the expression");}
	return 0;
}*/


